# מצב הפרויקט — EuroLeague Roster Optimization

**עודכן:** סוף יום 4 · 11.8.2026
**ריפו:** `github.com/Almog-Nissim/euroleague-roster-optimization`
**סוג המסמך:** מצב, לא יומן. מה נכון *עכשיו*, לא מה קרה מתי.

---

## 1. השאלה

> בהינתן מבנה עלות, איך ההקצאה על פני סגל מגיבה, וכמה היא רגישה למבנה.

**לא** "מה הסגל האופטימלי" — לזה אין דאטה ולא יהיה. הניסוח הזה נקבע ביום 3 כשמסלול B נבחר, והוא עומד.

**מועדון יעד:** מכבי תל אביב.
**עונת דמו (ניקוד):** 2025-26 — עונה שהסתיימה, כדי שהפלט ייבדק מול מה שקרה.
**חלון קלט:** עד 2024 בלבד.

---

## 2. הפירוק המרכזי

```
expected value = (PIR per game) × (expected games)
```

| גורם | מצב |
|---|---|
| **זמינות** (expected games) | ✅ בנוי, מכויל, מאומת OOS |
| **תפוקה** (PIR per game) | ❌ לא נבנה — אי-ודאות סביבו לא נאמדה |
| **עלות** | ⚠️ מפרט סגור, כיול לא הוכרע |

### מודל השרידות (יום 3, סגור)

```
P(survive) = logit⁻¹( -1.549 + 0.147·min_per_game − 0.049·(age − 27) )
```

- 484 שחקנים, 877 תצפיות, SE מקובצות לפי שחקן
- גיל ליניארי נכנס (ΔLL=+3.65, p=0.005); **גיל ריבועי נזרק (p=0.41)**
- כיול: פער מרבי 0.057 על עשירונים
- OOS: אימון 2022–23, מבחן 2024 → **0.664 חזוי מול 0.651 בפועל**

**הממצא המרכזי של יום 3:** בשוליים צעירים שורדים 47% — הקבוצה המסוכנת ביותר. בטבלה מוצלבת, **צעיר עם דקות רוטציה שורד ב-77.4%** — הגבוה ביותר בעמודה. ה-0.474 מדד ש-78 מתוך 114 הצעירים משחקים פחות מ-10 דקות. **ערבוב הרכב, לא תכונה.**

---

## 3. הדאטה

| קובץ | שורות | מצב |
|---|---|---|
| `team_season.csv` | 158 | ✅ 9 עונות, מאומת |
| `player_season.csv` | **2,553** | ✅ 9 עונות, 1,045 שחקנים |
| `player_features.csv` | **1,383** | ✅ 7 עונות יעד, אין דליפה |
| `salary_anchors.csv` | **109** | ✅ 4 שכבות |

**עונות:** 2016–2025, **2021-22 מושמטת** (אי-התאמה API/טבלאות, יום 2).

**אימות צולב:** סכום PIR לפי קבוצה, `player_season` מול `team_season` — פער מרבי **0.000000** על 158 קבוצות-עונה.

### `salary_anchors.csv` — שכבות

| `usage` | מי | n | מטבע | הערה |
|---|---|---|---|---|
| `calibrate` | מכבי 23/24, 24/25, 25/26 | 39 | USD | **סט הכיול היחיד** |
| `test` | הפועל 25/26 | 19 | USD | לא בכיול |
| `structure_only` | פנא' + אולימפיאקוס 25/26 | 38 | EUR | מוערך, טווחים 25–40% |
| `structure_only` | הפועל יורוקאפ 24/25 | 13 | USD | ליגה אחרת |

**אי-ודאות לפי מקור:** `reported` — 23% חולקים, פיזור 0.052 · `estimated` — 61% חולקים, פיזור 0.111. **המוערכים רועשים פי שניים.**

### מכפיל עלות מעביד — מדוד

| | n | ממוצע | טווח |
|---|---|---|---|
| זרים | 13 | 1.260 | 1.042–1.406 |
| ישראלים | 11 | 1.964 | 1.250–2.370 |

הממוצעים תואמים ל-1.25/2.0, **אבל הטווחים רחבים** — כלל אצבע שהמועדון חורג ממנו.

### מסלולי שכר (סט הכיול)

```
סורקין        437K → 850K → 900K
בלאט          400K → 650K → 700K
דיברתלומאו    337K → 650K → 650K
הורד                 400K → 1,050K
ריימן                225K → 225K
```

**קפיצה גדולה 2023→2024, קיפאון 2024→2025.** לא אינפלציה — התקציב *ירד* באותה שנה. שדרוג חוזה אחרי עונה מוצלחת.

---

## 4. החלטות סגורות

| החלטה | נימוק |
|---|---|
| **מסלול B** | שער העלות נפל על תנאי הכמות, לא האיכות |
| **מכבי כמועדון יעד** | ההמרה נטו→עלות היא מכפיל אחד ידוע |
| **נטו בלבד** | מכפילי לאום ל-Limitations |
| **דולר נומינלי** | אין תיקון מאקרו/FX. שינויים ברמת השחקן נשלטים ע"י חוזים אישיים |
| **אין אילוץ לאום** | היורוליג לא מגבילה זרים (תקנון) |
| **≤16 בו-זמנית** | אילוץ הספירה היחיד. 20 = תקרת תחלופה עונתית; 12 = לכל מפגש, מובלע בדקות |
| **הפועל בסט האימון** | עם דגל הקשר, לא מוצאת כחריגה |
| **שער העלות לא נפתח מחדש** | 25/26 היא עונת המבחן — כיול עליה הורס אותה |
| **רזולוציה: שחקן בודד** | 20 מתוך 28 עוגנים השוואתיים תואמים בדיוק |

---

## 5. מבנה העלות ל-PuLP

```
Σ cost(i)·x(i) ≤ B
Σ x(i) ≤ 16
```

### CBS (`cbs.py`, יום 4)

| | |
|---|---|
| ALCDR 25/26 | €19,489,944 |
| רצפה (LRL, רישיון A) | €5,846,983 |
| **תקרת מס (BRL)** | **€10M נטו** |
| HRL €12M | מושהה עד 27/28 |

**חריגות:** שלושת המשתכרים הגבוהים פטורים לחלוטין · מעל 3 עונות במועדון — 50% הנחה · מתחת לגיל 23 — פטור.

**התובנה:** בסגל לדוגמה, **85% מהשכר מוחרג.** החרגת העוגנים היא **סובסידיה לריכוזיות** — מועדון שמפזר 14M על 12 שחקנים משלם מס; מועדון שנותן 11M לשלושה לא משלם כלום.

זה מסביר את הדאטה: פנא' 7.5× בין #1 לחציון, הפועל 6.0×, **מכבי 3.7×** — מכבי משלמת יותר מס על אותו תקציב.

**⚠️ `VERIFIED = False`** — שתי דוגמאות המס בדוח סותרות זו את זו:

| חייבים | marginal | flat | הדוח |
|---|---|---|---|
| 12M | 1.25M | 1.50M | 1.50M |
| 20M | 13M | 20M | **40M** |

אף פרשנות לא משחזרת את שתיהן. **לא לשמש כאילוץ מחייב עד אימות מול התקנון.**

---

## 6. פונקציית העלות — מצב פתוח

### העיקרון (סגור)

> **עלות היא פונקציה של העבר. ערך הוא תחזית לעתיד.** הפער ביניהם הוא כל מקור האות.

`cost(i)` נבנית מ-PIR של t−1 ו-t−2 **בלבד**. PIR של עונת היעד לא נכנס לאגף העלות.

### המפרט שאושר (מלא)

```
log(cost) = β₀ + β₁·pir_lag_shrunk + β₂·(age−27) + β₃·(age−27)²
          + β₄·min_lag + β₅·I(el=2) + β₆·I(el=3) + β₇·I(el≥4)
```

**תחזיות כתובות מראש:**

| פרמטר | תחזית |
|---|---|
| `β₁` | חיובי, דומיננטי |
| `β₃` (age²) | **שלילי** — שיא מחיר ב-27, **הפוך מצד השרידות** |
| `β₅₋₇` | חיוביים ועולים, הקפיצה הגדולה בין 1 ל-2 |
| ספירמן מול PIR 2025 | 0.60–0.80 |

`β₃ < 0` הוא **מנוע המציאות**: בני 30–32 יקרים מדי (שוק משלם prime, מודל מנכה שרידות), בני 23–25 עם דקות רוטציה זולים מדי.

### מה שרץ בפועל (מצומצם)

```
log(share) = β₀ + β₁·pir_lag_shrunk + β₂·age_c² + β₃·el_seasons
share = salary / team_payroll(season)
```

מהלך הנתח מסלק אפקט עונה **בעלות אפס פרמטרים** — התקציב נע 7.74M → 6.31M → 10.80M.

**תוצאה (n=16 בסביבת בדיקה מוקטנת):**

| תחזית | בפועל |
|---|---|
| `β₁` חיובי | **0.252, p=0.016** ✅ |
| `β₂` שלילי | +0.005, p=0.45 — לא מובהק |
| `β₃` חיובי | −0.002, p=0.99 — אפס מידע |
| ספירמן 0.60–0.80 | 0.803 (מעל, אך < 0.90) |

**חוסר יציבות:** סימן `el_seasons` מתהפך בין k=20 ל-k=60 · בלי 2025 נשארות 7 תצפיות ו-`β₁` קופץ ל-0.69.

**עלות בפועל מול PIR = 0.490** — השוק מתמחר תפוקה **חלש יותר** מהמודל. המודל מוגזם, לא ריק.

### 🔴 המחלוקת הפתוחה

**המפרט המצומצם ויתר על:** `min_lag`, `age_c` ליניארי, `el_seasons` קטגוריאלי (3 דאמי → רציף).

**הספק שהועלה:** *האם נכון לרוץ על 3 משתנים בלי הדברים החשובים שמצאנו?*

זו **לא החלטה סגורה.** האילוץ הוא `n`, לא העדפה. שלוש דרכים:

1. **להישאר מצומצם** — עומד בכלל ה-10, מוותר על מנגנונים
2. **מפרט מלא בכל זאת** — מפר את הכלל, רווחי סמך שיכללו אפס
3. **מסלול B** — מקדמים מוצהרים לכל הגורמים, רגרסיה רק כמבחן סימנים

**המפרט המלא נשאר ההצהרה הרשמית.** המצומצם הוא מה שהמדגם מרשה כרגע.

---

## 7. פרמטרים מוצהרים

| פרמטר | ברירת מחדל | טווח רגישות |
|---|---|---|
| משקלי lag (t−1/t−2) | 0.6 / 0.4 | 0.7/0.3 · 0.5/0.5 |
| `k` (התכווצות) | 40 | 20 · 60 |
| מרכוז גיל | 27 | — |
| `el_seasons` top-code | 4+ | — |

**התכווצות:** `w = games/(games+k)`. `median_w ≈ 0.53` — השחקן החציוני מקבל ~47% מממוצע הליגה. בדלי `<20 משחקים` סטיית התקן קורסת מ-3.71 ל-0.84.

---

## 8. באגים שתוקנו (יום 4)

שלושתם היו **שקטים** — החזירו 0% התאמה, לא שגיאה.

1. **`player_code` הוא מחרוזת** — 36 קודים אלפאנומריים (`LCZ`, `JDR`, `TGB`), שייכים לוותיקים
2. **פורמט לא יציב** — אפסים מובילים מופיעים ונעלמים בין builds. תוקן בשורש (`normalise_code()`)
3. **`paths.py` החזיר `str`** — תוקן ל-`pathlib`

---

## 9. פריטים פתוחים

### חוסמים

- 🔴 **עמדות שחקנים** — לבדוק `player_stats` ב-`euroleague_api`. **סומן "לא חוסם" ביום 3 ועדיין לא נבדק.** בלי עמדות אין אילוץ סגל אמיתי ב-PuLP
- 🔴 **הכרעת המפרט** (סעיף 6) — מצומצם / מלא / מסלול B
- 🔴 **אימות מדרגות CBS** מול תקנון היורוליג

### דאטה

- 🟡 **עוד סגלים** — מועדון אירופי בינוני/קטן (ז'לגיריס, ורז'ה, באסקוניה) שווה יותר מגדול. מכבי 22/23. **רשימות Top 10 לא עוזרות** — נופלות על אותו תנאי
- 🟡 `seasons_at_club` — הרגולציה מתמחרת ותק **במועדון**, לא ביורוליג. הדאטה קיים
- 🟡 אי-ודאות סביב `pir_per_game` — הגורם השני בפירוק, לא נבנה

### תשתית

- 🟢 `__init__.py` + הרצה כמודול, במקום `sys.path.insert` בכל קובץ ב-`audits/`
- 🟢 `dtype={"player_code": str}` בסקריפטים ישנים
- 🟢 `os.path` → `pathlib` בשאר הריפו
- 🟢 קבצי `salary_anchors.csv` יתומים ב-`raw/` וב-`src/`
- 🟢 מכסת שחקנים מקומיים בליגה הישראלית — אין שדה לאום
- 🟢 מכבי 25/26 שיחקה בנסיבות חריגות (בית מחוץ לישראל) — שורה ב-Limitations

---

## 10. עקרונות עבודה

- **קובץ מת ברפו הוא מוקש**, לא רעש
- **שוליים משקרים כשהצירים מעורבבים** — "צעירים שורדים 47%" היה נכון ומטעה לחלוטין
- **לחדד דליים לפני שרואים תוצאה**, לא אחריה
- **עקביות בדחיית הסברים** — הצניחה ב-20–25 והגבנון ב-27–30 קיבלו אותה תשובה: בטווח הרעש
- **כיול חשוב יותר מדירוג** — מודל שמדרג נכון ומשקר על הרמה הורס אלף סימולציות
- **כלל לפני מקרה** — תנאי העוגנים נוסחו לפני החיפוש
- **הודעת הצלחה שלא בודקת את מה שהיא מדווחת עליו היא באג** — כל סקריפט זורק ולא מדפיס OK
- **איכות לפני מודל** — הרחבת הדאטה ל-9 עונות תיקנה מבנה שגוי לפני שנבנה עליו משהו

---

## 11. הצהרות ל-Limitations

- `el_seasons` נספר מ-2016; ותיק עם 12 עונות ושחקן עם 8 באותו תא
- 2021-22 מושמטת — הספירה היא של עונות **זמינות**
- דולר נומינלי, אין תיקון FX
- נטו בלבד, מכפילי לאום לא מיושמים
- 20 הסגלים האמיתיים מוגדרים לפי מי ששיחק, ולכן ממשים 100% בהגדרה. **סגל האופטימייזר לא** — כל יתרון יושג למרות זה
- מאגר מועמדים: מי ששיחק ביורוליג בחלון. חתימות מחוץ ליורוליג מחוץ להיקף
- שחקנים שלא נרשמו לגיליון היורוליג נכללים בתקציב, תורמים אפס PIR