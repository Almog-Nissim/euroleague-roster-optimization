# METHODS — כל כלי סטטיסטי בפרויקט, בעברית פשוטה

**עודכן:** 13.9.2026 · הועבר לספק `market`
**למה זה קיים:** יש בפרויקט הרבה מבחנים, וקל לאבד את החוט. המסמך הזה עונה על שלוש שאלות לכל כלי — **מה הוא שואל · למה הוא ולא אחר · איפה הוא בקוד**.

לא תיאוריה. רק מה שאנחנו משתמשים בו בפועל.

> **מה השתנה מאז גרסת יום 7.** `COST_SPEC=market` הוא הייצור (ADR 0001), ולכן מודל
> העלות כאן הוא `cost_market.py` ולא `cost_model.py`, והמספרים שלו הוחלפו. נוספו
> שלושה כלים שלא היו: **אילוץ זהות הכדור**, **אילוץ צורת הדקות** (ADR 0005),
> ו-**קונבנציית הניקוד** (ADR 0005/0006) — שהיא הכללה של השיעור המרכזי של יום 7.
> `METHODS.pdf` בשורש נוצר מהגרסה הקודמת ולא מזו.

---

## 0. מפה מהירה

| כלי | מה הוא עונה | איפה |
|---|---|---|
| OLS / רגרסיה | כמה X מסביר את Y | `cost_market.py` |
| אפקטים קבועים | להוציא הבדלי רמה בין מועדונים ועונות | `cost_market.py` |
| טרנספורמציית log | להפוך יחסים כפליים לחיבוריים | `cost_market.py` |
| WLS (משוקלל) | רגרסיה כשלא כל תצפית שווה | `production_model.py` |
| GLM בינומי | רגרסיה כש-Y הוא **שיעור** בין 0 ל-1 | `avaliabillity_model.py` |
| התכווצות | לרסן אומדנים ממדגם קטן | `roster_optimizer.py` |
| כיול | האם התחזיות **ברמה** הנכונה | הכל |
| OOS | האם זה עובד על מה שלא ראינו | `Oos_hapoel.py` |
| מונטה קרלו | להריץ עונה 400 פעם | `roster_optimizer.simulate` |
| פיזור-יתר / בטא-בינומי | האם ה**שיעור** עצמו לא ידוע | `avail_uncertainty.py` |
| תכנון לינארי בשלמים (PuLP) | לבחור סגל תחת אילוצים | `optimise_consistent.py` |
| סווייפ רגישות | כמה התשובה תלויה בהנחה | `depth_sweep.py` |
| אילוץ זהות | לאכוף זהות שחייבת להתקיים | `usage_constrained.py` |
| אילוץ על צורה | לחסום צירוף קצוות, לא כל קצה לחוד | `scoring.py` |
| קונבנציית ניקוד | על איזה מידע כל צד עומד | `scoring.py` |
| תחזית נעולה מראש | למנוע התאמה בדיעבד | `Oos_hapoel.py` ואילך |

---

## 1. מה שכבר הכרת

### רגרסיה (OLS)

```
log(salary_i) = a_מועדון + g_עונה + b1·pir_lag_shrunk + b2·el_seasons
```

**מה שואלים:** בהינתן PIR של אשתקד, מה השכר הצפוי.
**מה שקוראים:** `β₁` הוא הכיוון והעוצמה. `p` הוא הסיכוי לקבל תוצאה כזו במקרה — קטן מ-0.05 נחשב "לא מקרי". `R²` הוא כמה מהשונות הוסברה.

**אצלנו, תחת `market`:** `β₁ = 0.1727`, מ-`refit_acceptance.csv`.

**ולמה זה הקואפיציינט היחיד שחשוב.** כל השאר במודל העלות — החותך, ה-`smear`,
אפקטי המועדון, ונרמול ממוצע המאגר — מכפילים את המחיר של **כל** שחקן באותו גורם.
הכפלת כל העלויות והתקציב באותו גורם משאירה את הקבוצה הפיזיבילית של ה-LP כמו
שהיא, ולכן אינה יכולה לשנות מי נבחר. `β₁` קובע כמה תלול המחיר עולה עם תפוקה,
והוא נושא כמעט את כל פיזור המחירים.

⛔ המספרים שהיו כאן — `β₁ = 0.252, p = 0.016`, `R² = 0.953` על מכבי ו-`0.709` על
הפועל — הם כיול **מועדון בודד** מלפני ADR 0001. הם אינם מתארים את הייצור.

### טרנספורמציית log

**למה:** שכר גדל **כפלית** — מ-300K ל-600K זה "פי 2", כמו מ-3M ל-6M. רגרסיה רגילה מניחה תוספת קבועה. `log` הופכת כפל לחיבור, ואז הרגרסיה מתאימה.
**מחיר:** כשחוזרים ל-`exp` מקבלים **חציון**, לא ממוצע. לכן יש `smear` בקוד — תיקון שמחזיר אותנו לממוצע.

### אפקטים קבועים

**הבעיה:** מכבי משלמת אחרת מ-ASVEL על אותו שחקן, ו-2019 אחרת מ-2025. בלי להוציא
את הבדלי הרמה האלה, `β₁` סופג אותם ומודד "אילו מועדונים עשירים" במקום "כמה
תפוקה מייקרת שחקן".

**הפתרון:** מקדם נפרד לכל מועדון ולכל עונה. ההשוואה נעשית **בתוך** מועדון-עונה.

**מה זה קונה:** אפשר לתמחר שחקן ב"מועדון ממוצע בליגה" במקום במועדון הכיול. וזו
הסיבה ש-`cost` **חסר יחידות** — המחיר מחולק בממוצע המאגר של העונה, כך שיחידה
אחת היא "שחקן מאגר ממוצע אחד". **אין לקרוא `cost` כסכום כספי.** ציר היורו נבדק
ונדחה, ראו `docs/refit-day15-diff.md`.

**מחיר שנרשם:** הפיצול לכיול ולבדיקה הוא **לפי מועדון ולא לפי שורה**. מועדון
שלם הולך לצד אחד, כי אפקט קבוע של מועדון שנאמד על אותן שורות שבודקות אותו אינו
מבחן חוץ-מדגמי.

### משתנה קטגוריאלי

`el_seasons` — מספר עונות ביורוליג. אפשר להתייחס אליו כמספר רציף (כל עונה מוסיפה אותו דבר) או כקטגוריות (עונה 1, 2, 3, 4+ נפרדות).
**איפה זה עומד:** ספק ה-`market` רץ **רציף**, עם קואפיציינט אחד `β₂`.
`ro.COST_FEATURES` הוא בדיוק שתי תכונות — `pir_lag_shrunk` ו-`el_seasons` — ולא
שבע. המחלוקת אינה מוכרעת ב-ADR; היא פשוט אינה חיה, כי הנוסחה שרצה בייצור היא זו
שלמעלה. ⛔ הנוסחה הארוכה שהופיעה כאן וב-`project_state.md`, עם `age_c`, `age_c²`,
`min_lag` ודאמיז ל-`el`, אינה מה שרץ.

### סטיית תקן

הפיזור סביב הממוצע. **החשוב:** אנחנו מבחינים בין **שני סוגים** —

| | מה זה | דוגמה |
|---|---|---|
| **שארית** | הפיזור האמיתי בין שחקנים | שחקנים באמת שונים זה מזה |
| **שגיאת אמידה** | כמה אנחנו לא בטוחים בתחזית | מדגם קטן → לא בטוחים |

**זה היה באג ביום 6:** ענשנו שחקנים לפי **השארית** — כלומר הענשנו שחקן שבאמת טוב יותר מהתחזית. אבל **הוא מה שמחפשים.** קללת המנצח נובעת משגיאת ה**אמידה**. התיקון: מעבר ל-`se_mean`.

---

## 2. מה שהוספנו

### WLS — רגרסיה משוקללת

**הבעיה:** שחקן ששיחק 34 משחקים ושחקן ששיחק 3 — ה-`ppm` של הראשון אמין הרבה יותר.
**הפתרון:** לתת לכל תצפית **משקל**. אצלנו המשקל הוא סך הדקות.
**איפה:** `production_model.py`, `sm.WLS(..., weights=minutes_tot)`

### GLM בינומי — כש-Y הוא שיעור

**הבעיה:** זמינות היא `משחקים / משחקים אפשריים` — תמיד בין 0 ל-1. רגרסיה רגילה יכולה לנבא 1.3 או −0.2, וזה חסר משמעות.
**הפתרון:** מודל שמובנה כך שהתוצאה לא יכולה לצאת מהתחום. הקשר הוא `logit`:

```
logit(p) = log( p / (1−p) )
```

הצד השמאלי חופשי לנוע מ-−∞ ל-+∞, והצד הימני תמיד בין 0 ל-1.

**אצלנו:** `logit(frac) = −0.962 + 0.056·דקות_קודם + 1.456·זמינות_קודם − 0.008·גיל`
**איפה:** `avaliabillity_model.py`

### התכווצות (Shrinkage)

**הבעיה:** שחקן ששיחק 3 משחקים עם PIR 20 — האם הוא באמת כזה, או שהיה לו מזל?
**הפתרון:** למשוך את האומדן שלו לכיוון ממוצע הליגה, **ככל שיש עליו פחות דאטה**.

```
w = משחקים / (משחקים + k)          k = 40
אומדן = w·שלו + (1−w)·ממוצע_הליגה
```

3 משחקים → `w = 0.07`, כלומר 93% ממוצע ליגה. 34 משחקים → `w = 0.46`.
**זה פרמטר מוצהר**, ו-`k` נסרק ב-20 ו-60 כבדיקת רגישות.

### כיול — ולמה הוא חשוב יותר מדירוג

**דירוג:** האם סידרנו את השחקנים נכון.
**כיול:** האם ה**רמה** נכונה.

מודל שאומר "כולם יהיו זמינים 90%" כשבפועל זה 75% — **מדרג מושלם ומשקר על הרמה**. וכשמכניסים אותו לאופטימייזר שמחלק תקציב, כל ההקצאה שגויה.

**איך בודקים:** מחלקים את השחקנים לעשירונים לפי התחזית, ובכל עשירון משווים תחזית ממוצעת מול מה שקרה. **הפער המרבי** הוא הציון.

**אצלנו:** זמינות — פער 0.054 מול סף 0.070 שנרשם מראש ✅ · תפוקה — 0.096 מול 0.05 ❌ **נכשל**

### OOS — מחוץ למדגם

**הרעיון:** לאמן על 2023 ומטה, לנבא את 2024, ולהשוות למה שקרה. מודל שנראה מצוין על הדאטה שלו ונופל מחוץ לו — **התאים את עצמו לרעש**.

**אצלנו:** זמינות — 0.753 חזוי מול 0.740 בפועל. פער +0.013 ✅

### מונטה קרלו

**הרעיון:** במקום לחשב "מה יקרה בממוצע", להריץ את העונה 400 פעם עם הגרלות ולראות את **ההתפלגות**.

**מה מוגרל אצלנו, ובאיזה סדר:**

```
פעם אחת לעונה :  ppm ~ N(תחזית, סטיית_תקן)     — השחקן הוא מה שהוא
בכל משחק      :  נוכח / לא נוכח
```

**הסדר הזה הוא כל העניין.** אם הכוכב מתגלה חלש מהצפוי, זה נכון **לכל העונה**, וצריך למי להעביר את הדקות. זה מה שנותן לעומק ערך, ומה שהחישוב הדטרמיניסטי לא רואה.

### פיזור-יתר ובטא-בינומי

**השאלה:** ה-GLM נותן "לשחקן הזה 78% זמינות". אבל **כמה אנחנו בטוחים במספר הזה עצמו?**

**המבחן:** אם המודל מושלם, הסטייה של כל תצפית מהתחזית צריכה להיות בגודל שהמודל הבינומי צופה. מחשבים

```
φ = ממוצע [ (בפועל − חזוי)² / השונות_הצפויה ]
```

`φ = 1` → אין אי-ודאות נוספת. `φ = 10.38` (מה שקיבלנו) → **השונות גדולה פי עשרה ממה שהמודל מניח.**

**התיקון — בטא-בינומי:** קודם מגרילים את השיעור האמיתי של השחקן מהתפלגות בטא סביב התחזית, ורק אז מגרילים משחק-משחק.

**אצלנו:** ס"ת של השיעור האמיתי סביב 0.78 הוא **0.228** — שחקן שנחזה 78% הוא בפועל בטווח 55%–100%.
**איפה:** `avail_uncertainty.py`

### תכנון לינארי בשלמים (PuLP)

**מה זה:** בחירה תחת אילוצים. משתני החלטה בינאריים (`x_i` = בסגל או לא) ורציפים (`e_i` = דקות), ומקסמים ביטוי לינארי.

```
max  Σ ppm(i)·e(i)
     Σ e(i) ≤ 200                  ·  e(i) ≤ 32·זמינות(i)·x(i)
     Σ עלות(i)·x(i) ≤ תקציב
     12 ≤ Σ x(i) ≤ 16              ·  רצפות ותקרות עמדה
     Σ e(i)·(usage(i) − 20) ≤ 0    ·  זהות הכדור, מסלול הכותרת
     סכום k הדקות הגדולות ≤ C_k    ·  צורת הסבב, לכל k עד 8 (ADR 0005)
```

**הכלל הקריטי שנלמד ביום 7:** **הפונקציה שממקסמים חייבת להיות בדיוק הפונקציה שמנקדת.** אם לא — הידוק אילוץ יכול **להעלות** את הערך, וזה בלתי אפשרי מתמטית. זו הייתה ההוכחה לבאג.

### אילוץ זהות — כשמספר חייב להתקיים

**הרעיון:** לא כל אילוץ הוא הנחה. `usage` הוא נתח ההחזקות מתוך אלה של הקבוצה,
והממוצע המשוקלל-דקות שלו **חייב** להיות 20.0% — חמישה על המגרש, כדור אחד. זו
זהות, לא אמידה, ולכן אין לה סף להצדיק.

```
Σ e(i)·usage(i) ≤ 20·Σ e(i)    <=>    Σ e(i)·(usage(i) − 20) ≤ 0
```

ליניארי לגמרי, בלי משתנים חדשים. **הסתייגות שנרשמת:** `usage` נלקח מהעונה
**הקודמת**, כי זה מה שידוע בזמן הבחירה. כלומר האילוץ אוכף שהסגל היה עומד בזהות
*אילו כל שחקן שמר על נתחו* — נכון כאילוץ תכנוני, לא כניבוי.

### אילוץ על צורה — לא כל קצה לחוד

**הבעיה, והיא חזרה שלוש פעמים.** תקרת 32 הדקות חלה על כל שחקן **בנפרד**, ולכן
האופטימייזר נותן אותה לכולם בו-זמנית: 192 דקות לשישייה, מול **145.3** של הקבוצה
הכי מרוכזת ב-38 עונות-מועדון. אותו דבר קרה עם רצפות העמדה. בשני המקרים לקחנו
קצוות שוליים, כל אחד מקבוצה אחרת, והרכבנו מהם קבוצה שאיש לא שיחק בה.
**אופטימייזר תמיד יילך לפינה.**

**הפתרון:** לחסום את **סכום k הגדולים** לכל k, ולא כל שחקן לחוד. זה ניתן לביטוי
ליניארי בזהות תקנית (Nesterov), ולכן נכנס ל-LP בלי לשבור אותו:

```
סכום k הגדולים של e ≤ C_k
<=>  קיימים q, s≥0 כך ש:   k·q + Σ s(i) ≤ C_k ,   s(i) ≥ e(i) − q
```

**מה נמדד:** האילוץ כובל, ומוריד את הכותרת ב-14.8%. ה-`k` שכובלים הם 4 עד 8
(`k=8` ב-97% מהפתרונות), ו-`k=1,2,3` **לא כובלים אף פעם** — ההנחה הקודמת
ש"k סמוכים כמעט מיותרים" הייתה שגויה בשני הכיוונים.

**איפה:** `scoring.add_shape_constraint` — מימוש אחד שאותו קוראים שני
האופטימייזרים. שני מימושים לאילוץ אחד הוא איך שנוצרו שתי פונקציות ניקוד.

### קונבנציית ניקוד — על איזה מידע כל צד עומד

**ההכללה של השיעור מיום 7.** שם למדנו שהמטרה חייבת להיות פונקציית הניקוד. הכלל
הרחב יותר הוא ש**שני צדי ההשוואה חייבים לעמוד על אותו מידע.**

הסקורר המקורי מחלק דקות בסדר יורד של `ppm_true` — כלומר **בדיעבד**, בידיעה מי
יצא טוב. כל זמן ששני הצדדים קיבלו את זה, זו הייתה סימטריה. אבל סימטריה שמייפה
את המועדון: היא מעניקה לשישייה שלו **177.0** דקות במקום **125.7** ששיחקה, מכנה
מנופח ב-40.8%.

```
א. שני הצדדים מחולקים מחדש בדיעבד      סימטרי · מייפה את המועדון · 0.1414
ב. המועדון על מה ששיחק, המנוע בדיעבד    א-סימטרי · +98.4% · נעצר (ADR 0005)
ג. שני הצדדים על מה שהתחייבו אליו       ADR 0006 · התוצאה טרם נמדדה
```

**(ב) נעצר בכלל שהוצהר מראש.** הוא מערבב את הטענה של הפרויקט — בחירת סגל טובה
יותר באותו תקציב — עם טענה שהוא אינו טוען: ניהול דקות טוב יותר ממאמנים, עם
ידיעת התוצאה.

**(ג) מנקד את המנוע על `min(e_LP, 32·זמינות_בפועל)`** — התוכנית שהתחייב אליה,
חתוכה לפי הזמינות שהתממשה — ואת המועדון על הדקות ששיחק. הדקות שהתפנו **אינן**
מחולקות מחדש; הן נופלות לרמת המחליף, וזה מחיר הסיכון של לתכנן סביב שחקן שלא היה
שם. נמדד: חציון **27.3** דקות מ-200 נקצצות כך.

### סווייפ רגישות

**מתי:** כשמספר בקוד הוא הנחה ולא מדידה.
**מה עושים:** מריצים על פני טווח שלם ומדווחים **את הטווח**, לא ערך אחד.

**דוגמה חיה:** רמת המחליף. מדדנו 0.127, אבל היא נעה בין 0.030 ל-0.179 לפי הסף שבוחרים. לכן היא **נסרקת ולא נבחרת**.

### תחזית נעולה מראש

**לא כלי סטטיסטי — כלל עבודה, והוא החשוב ביותר בפרויקט.**

לפני שמריצים, כותבים בראש הקובץ **מה אנחנו צופים ולמה**. אחר כך אי אפשר להתאים את הפרשנות לתוצאה.

**זה עבד:** ביום 6 חזיתי שאצל הפועל התקציב לא מגביל. ניצול היה 99.7%. **התחזית נפלה, וזה היה הממצא.**
**וזה נשמט:** בבוקר יום 7 הרצתי שלושה קבצים בלי אף תחזית. הבוקר הזה הוא היחיד שכל מסקנותיו נפלו.

---

## 3. הכשלים החוזרים — מה לבדוק לפני שמדווחים מספר

| הכשל | דוגמה בפרויקט |
|---|---|
| **שני צדי ההשוואה אינם אותם אנשים** | בנצ'מרק מכבי — 9 שחקנים במקום 12, בלי שני הטובים |
| **סקלה מעונה אחרת** | תמחור מועמדי 2024 לפי שכר ממוצע של 2023 |
| **מכנה לא נבדק** | לויד — 22.8 דק' למשחק אחרי משחק אחד |
| **מזהה נחשב למספר** | `gamecode`, `player_code`, `Player_ID` — שלוש פעמים |
| **מבחן שלא יכול להראות אחרת** | בחירת גודל סגל לפי ניקוד שמונוטוני יורד |
| **כישלון שקט** | `Skip and continue` · `.gitignore` שחוסם דאטה |
| **סינון חסר** | בוקסקור כולל פלייאוף, `player_season` לא |
| **`inner join` על עמודה ריקה** | בקרת התקציב רצה על `gross_eur`, ריק ב-20/20 מועדוני 2025 — **כל העונה נשרה בשקט** |
| **סף שנוסח לציר אחד ונבדק על אחר** | תנאי 1 ב-`refit_acceptance` בחן `a=1.443` מהציר המנורמל, מול סף שנוסח ל-`a=1.087` ברמת היורו |
| **עמודה שמבטיחה דאטה שאינו שם** | `minutes_alloc` ריק ב-456/456, כי ההיוריסטיקה חיפשה מערך בערך ההחזרה של `score_rows` — שמחזירה שלושה סקלרים |
| **התניה חסרה** | מיצוע דקות כולל משחקים שבהם השחקן נעדר |

**ארבעה מתוך שמונה הם אותה משפחה:** דיווחתי מספר לפני שווידאתי ששני הצדדים עומדים על אותו בסיס.

---

## 4. איפה כל מבחן יושב

| קובץ | מה הוא בודק |
|---|---|
| `cost_market.py` | 🔴 **מודל העלות של הייצור** — OLS עם אפקטי מועדון ועונה |
| `cost_model.py` | הספק הקודם, `club_relative`. קיים רק לשחזור מספרי יום 14 |
| `production_model.py` | WLS — `ppm` צפוי |
| `avaliabillity_model.py` | GLM בינומי — שיעור משחקים |
| `audits/survival_model.py` | שרידות (הוחלף, לא נזרק) |
| `Oos_hapoel.py` | מבחן חוץ-מדגמי עם תחזיות נעולות |
| `league_jump_test.py` | הליגה הישראלית — הטיית בחירה |
| `roster_optimizer.py` | PuLP + מונטה קרלו |
| `optimise_consistent.py` | 🔴 יישור המטרה לניקוד |
| `optimizer_backtest.py` | הבקטסט של המנוע |
| `backtest_diagnostics.py` | שלוש בדיקות על **המבחן** |
| `roster_membership_audit.py` | מי נחשב לסגל |
| `benchmark_matched.py` | ההשוואה מול המועדון |
| `curse_decomp.py` | פירוק קללת המנצח |
| `replacement_level.py` | מדידת רמת המחליף |
| `depth_sweep.py` | סווייפ רגישות |
| `split_multiclub.py` | פיצול לפי מועדון מבוקסקור |
| `avail_uncertainty.py` | פיזור-יתר בזמינות |
| `curse_redistribution.py` | קללה — ממצא או ארטיפקט |
| `absence_position_test.py` | לאן דקות הנעדר הולכות |
| `usage_constrained.py` | 🔴 **מסלול הכותרת** — אילוץ זהות הכדור |
| `scoring.py` | אילוץ הצורה וקונבנציות הניקוד (ADR 0005/0006) |
| `shape_constraint_test.py` | הטסטים של אילוץ הצורה |
| `league_backtest.py` | הבקטסט על 38 עונות-מועדון |
| `fatigue_delta.py` | אמידת δ העייפות — הופרך על ראיות (ADR 0004) |
| `null_to_wins.py` | מודל האפס |
| `wins_conversion.py` | המרת ניקוד לניצחונות |
| `refit_acceptance.py` | שערי הקבלה של הריפיט |
| `refit_diff.py` | טבלת לפני/אחרי, מקבצי תוצאה מקומטים |
| `final_day7.py` | ההרצה המסכמת |
